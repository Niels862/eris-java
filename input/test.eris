class Point {
    x: int;
    y: int;
}

func make_point(x: int, y: int) -> Point {
    var p = Point(x, y);
    p.x = x;
    p.y = y;
    return p;
}

func main() -> int {
    return make_point(3, 5).x;
}
