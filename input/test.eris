class Point {
    x: int;
    y: int;
}

func make_point(x: int, y: int) -> Point {
    var p = Point(x, y);
    p.x = x;
    p.y = y;
    return p;
}

func f(x: int?) -> int {
    if x !== null {
        x = null;
        return x;
    }
    return 0;
}

func main() -> int {
    var x = make_point(3, 5).x;
    return f(x);
}
