class Point {
    x: int;
    y: int;
}

func make_point(x: int, y: int) -> Point {
    return Point(x, y);
}

func main() -> int {
    point: Point = make_point(3, 5);
    point.x = 7;
    return point.x;
}
